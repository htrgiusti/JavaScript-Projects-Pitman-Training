function displayDate() {
    document.getElementById("demo").innerHTML = Date ()
}

function myFunction() { //Creates a function called "myFunction".
    var sentence = "I am learning"; //Creates a string variable.
    sentence += " a lot from this book!";//Creates another string variable with += to concatenate the text.
    document.getElementById("Concatenate").innerHTML = sentence;//this method gets the id "Concatenate" in the HTML file. 
}

